// resource.h — resource IDs shared between app.rc and the C source.
// Kept deliberately free of any C-only declarations (no windows.h, no
// function prototypes) so the resource compiler (windres/rc.exe) can parse
// this file directly without choking on it.
#ifndef RESOURCE_H
#define RESOURCE_H

#define IDI_APPICON 101

#endif
